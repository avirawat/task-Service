server:
  port: 8072
  
eureka:
  client:
    service-url:
     default-zone: http://localhost:8761/
     
spring:
  application:
   name: TASK-SERVICE
  config:
    import: configserver:http://localhost:8888